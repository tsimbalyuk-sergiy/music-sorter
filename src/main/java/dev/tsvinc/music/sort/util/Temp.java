package dev.tsvinc.music.sort.util;

public class Temp {
  public static void main(String[] args) {

    int minFacesCount = 1;
    int maxFacesCount = 2;
    final var format = String.format("Wrong min_faces_count/max_faces_count values. min: %s, max: %s", minFacesCount, maxFacesCount);
    System.out.println(format);
  }
}
